#include <stdio.h>
#include <conio.h>
double produit (double x, double y)
	{
		return x*y ;
	}
	int main (int argc, char**argv)
	{
		double x,y ;
		printf (« Entrez valeur : ») ;
		scanf (« %f%f », &x, &y) ;
		printf (« Resultat :%f\n », produit(x,y)) ;
		return o ;
	}

