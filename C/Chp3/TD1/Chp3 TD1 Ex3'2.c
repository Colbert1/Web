#include <stdio.h>
#include <conio.h>
int nb1, nb2, result;
{
	If(nb2 ! = 0)
	{
		return (nb1 / nb2);
	}
	return 1
}
void main()
{
	int nb1, nb2, result;
	printf(� Entrez deux nombres �);
	scanf(� % d �, &nb1);
	scanf(� % d �, &nb2);
	result = div(nb1)(nb2);
	printf(� % d �, result);
	getch() return 0;
}
