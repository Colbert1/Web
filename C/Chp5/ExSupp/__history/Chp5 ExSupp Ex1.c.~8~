#include <stdio.h>
#include <conio.h>
#include <stdlib.h>
#include <math.h>

int main(int argc, char* argv[])
{
	FILE *fichier;
	char caractere;
	int verif=0;

	fichier = fopen("./alphabet.txt","w");
    fichier = fprintf(FILE*fichier,char*

fclose(fichier);

getch();
return 0;
}
