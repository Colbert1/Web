#include <stdio.h>

int main()

		{
			char tableau[Max];
			char l;
			int x=0, a, j, k,repete = 0;
			printf("Entrez une chaine de caractères :\n");
			gets(tableau);
			printf("Choisissez une lettre : ");
			scanf ("%c", &l);
			for(a=0; a<strlen(tableau); a++)
		{

			x++;

   for( x = 0; x<a;x++){

if(tableau[a]==tableau[x]);
						}
		}





strrchr (tableau, l);
printf("La lettre %c est à la place %d en partant de la droite\n", l, x);


getchar();
}
